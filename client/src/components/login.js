import React, { useState } from "react";

const Login = (props) => {
  const {
    randomProps
  } = props;

  const [selected, setSelected] = useState("");

  return (
    <div>
        login
    </div>
  );
};


export default Login;
